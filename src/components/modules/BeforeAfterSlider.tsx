"use client";
import NodePlaceholder from "./NodePlaceholder";

export default function BeforeAfterSlider() {
    return (
        <NodePlaceholder title="03. Transformação" className="bg-purple-950/20">
            <p>Antes (Manual) vs Depois (Automático).</p>
        </NodePlaceholder>
    );
}
